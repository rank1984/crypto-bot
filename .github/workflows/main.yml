name: 🚀 CRYPTO-BOT

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          
      - name: Install dependencies
        run: pip install -r requirements.txt
        
      - name: Run bot
        env:
          TELEGRAM_TOKEN:  ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py --once

      - name: 🚀 Commit and Push Shadow Results
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          if [ -f shadow_results.csv ]; then
            git add data/shadow.db shadow_results.csv
            git diff --cached --quiet || git commit -m "📈 Auto-update shadow results [skip ci]"
            git pull --rebase -X ours origin main || true
            git push origin main
          else
            echo "shadow_results.csv not found, skipping commit."
          fi

      - name: 💾 Save Shadow Mode CSV Logs (Artifact Backup)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: shadow-results
          path: shadow_results.csv
          if-no-files-found: warn
          retention-days: 3
